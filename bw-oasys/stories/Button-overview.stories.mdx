import { Story, Canvas, ArgsTable, Template, Source } from "@storybook/addon-docs";
import { ButtonComponent } from 'projects/oasys-lib/src/public-api';
import { CssPropsBlock } from "@ljcl/storybook-addon-cssprops";
import cssprops from "./assets/tokens/tokens";
import dedent from 'ts-dedent';

<Meta title="Components/Actions/Button/Overview" component={ButtonComponent} />

export const DocsTemplate = (args) => ({ props: args });

# Button Types Overview

Showing all button color types in one place. Don't forget you can change brand at any time in the top toolbar ('image' icon on the right)

## Primary, Secondary, Tertiary – On Light

<Canvas style={{ background: '#fff' }}>
  <Story id="components-actions-button--primary">
      {DocsTemplate.bind({})}
  </Story>
  <Story id="components-actions-button--secondary">
      {DocsTemplate.bind({})}
  </Story>
  <Story id="components-actions-button--tertiary">
      {DocsTemplate.bind({})}
  </Story>
</Canvas>

## Primary, Secondary, Tertiary – On Dark

<Canvas style={{ background: '#262626' }}>
  <Story id="components-actions-button--primary-inverse">
      {DocsTemplate.bind({})}
  </Story>
  <Story id="components-actions-button--secondary-inverse">
      {DocsTemplate.bind({})}
  </Story>
  <Story id="components-actions-button--tertiary-inverse">
      {DocsTemplate.bind({})}
  </Story>
</Canvas>

## Expressive and Danger

<Canvas style={{ background: '#f7f7f7' }}>
  <Story id="components-actions-button--expressive">
      {DocsTemplate.bind({})}
  </Story>
  <Story id="components-actions-button--danger">
      {DocsTemplate.bind({})}
  </Story>
</Canvas>

## Partners

<Canvas style={{ background: '#f7f7f7' }}>
  <Story id="components-actions-button--facebook">
      {DocsTemplate.bind({})}
  </Story>
  <Story id="components-actions-button--paypal">
      {DocsTemplate.bind({})}
  </Story>
  <Story id="components-actions-button--trustpilot">
      {DocsTemplate.bind({})}
  </Story>
</Canvas>
