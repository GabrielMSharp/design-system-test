import { Story, Canvas, ArgsTable, Template } from "@storybook/addon-docs";
import { ButtonComponent } from 'projects/oasys-lib/src/public-api';

<Meta title="Usage Guidelines/Buttons" component={ButtonComponent} />

export const DocsTemplate = (args) => ({ props: args });

# Welcome to custom documentation in STORYBOOK!!!!

What's that, not excited? Well maybe you should sit down. Things bout to get craayze

It can be further expanded with your own code snippets and include specific information related to your stories.

## Overview

Buttons allow users to trigger an action or event with a single click. For example, you can use a button for allowing the functionality of submitting a form, opening a dialog, canceling an action, or performing a delete operation.

<Canvas>
  <Story id="design-system-components-actions-button--primary" />
</Canvas>

<Canvas>
  <Story id="design-system-components-actions-button--primary-2" />
</Canvas>

<Canvas>
  <Story id="design-system-components-actions-button--primary-3" />
</Canvas>

## Button Size

If you included the title in the story's default export, use this approach.

<Canvas>
  <Story
      id="design-system-components-actions-button--primary"
      name="Name of this component here"
      args={{
        buttonSize: 'small'
      }}>
      {DocsTemplate.bind({})}
  </Story>
  <Story
      id="design-system-components-actions-button--primary-3"
      name="Another name here"
      args={{
        buttonSize: 'large'
      }}>
      {DocsTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable of={ButtonComponent} />
