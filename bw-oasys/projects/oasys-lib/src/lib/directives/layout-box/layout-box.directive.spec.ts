import { LayoutBoxDirective } from './layout-box.directive';

describe('LayoutBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutBoxDirective();
    expect(directive).toBeTruthy();
  });
});
